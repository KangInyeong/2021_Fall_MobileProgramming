<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mobileprogramming">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileProgramming"
        android:label="Inyeong's profile">

        <activity
            android:name=".SubActivity"
            android:exported="true"
            tools:ignore="Instantiatable">

            <!--#hw2-->
            <!--This is the intent-filter to use implicit intent -->
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <data android:scheme="tel"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            tools:ignore="Instantiatable">

        </activity>

        //Homework3
        <activity android:name=".Hw3IntroActivity"
            android:exported="true">

        </activity>

        <activity android:name=".ProjectActivity"
            android:exported="true"></activity>

        <activity android:name=".Hw3ColorActivity"
            android:exported="true">

        </activity>

        <activity android:name=".Hw3ChangeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Hw3LuckyActivity"
            android:exported="true">


        </activity>

    </application>

</manifest>